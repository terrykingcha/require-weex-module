{
  "name": "require-weex-module",
  "version": "0.0.1",
  "description": "a function for requiring weex module in js file",
  "main": "index.js",
  "author": "terrykingcha <terrykingcha@gmail.com>",
  "repository": {
    "type": "git",
    "url": "git@github.com:terrykingcha/require-weex-module.git"
  },
  "licenses": "MIT",
  "keywords": [
    "weex",
    "loader",
    "weex-module"
  ],
  "scripts": {
    "clear": "rm -f lib/*",
    "lint": "eslint --fix src",
    "dev": "gazeer -p \"src/*.js\" -c \"npm run build\"",
    "build": "node_modules/babel-cli/bin/babel.js src --out-dir lib",
    "pretest": "npm run clear && npm run lint && npm run build",
    "test": "mocha test/test.js",
    "ci": "npm run test",
    "prepublish": "npm run clear && npm run lint && npm run build"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.10.4",
    "babel-preset-es2015": "^6.9.0",
    "chai": "^3.5.0",
    "eslint": "^2.13.1",
    "gazeer": "^0.1.1",
    "mocha": "^2.4.5",
    "sinon": "^1.17.3",
    "sinon-chai": "^2.8.0"
  },
  "dependencies": {}
}
